services:
  workspace:
    image: boilerplate-compile-rust-windows:stable
    build:
      context: ./docker
      dockerfile: Dockerfile.windows
    init: true
    working_dir: /workspace
    command: ["sleep", "infinity"]
    environment:
      CARGO_TARGET_DIR: /workspace/target
      CARGO_TERM_COLOR: always
      CARGO_BUILD_JOBS: "1"
      CARGO_BUILD_TARGET: x86_64-pc-windows-gnu
      CARGO_TARGET_X86_64_PC_WINDOWS_GNU_LINKER: x86_64-w64-mingw32-gcc
    volumes:
      - source:/workspace
      - target:/workspace/target
      - cargo_registry:/usr/local/cargo/registry
      - cargo_git:/usr/local/cargo/git
    ports:
      - "127.0.0.1:${REMOTE_PORT}:${APP_PORT}"
    mem_limit: 300m
    memswap_limit: 768m
    cpus: "0.50"
    labels:
      com.boilerplate-compile.managed: "true"
      com.boilerplate-compile.target: "windows"
      com.boilerplate-compile.project: "${PROJECT_ID}"

volumes:
  source:
    labels:
      com.boilerplate-compile.managed: "true"
      com.boilerplate-compile.target: "windows"
      com.boilerplate-compile.project: "${PROJECT_ID}"
  target:
    labels:
      com.boilerplate-compile.managed: "true"
      com.boilerplate-compile.target: "windows"
      com.boilerplate-compile.project: "${PROJECT_ID}"
  cargo_registry:
    external: true
    name: boilerplate-compile-cargo-registry
  cargo_git:
    external: true
    name: boilerplate-compile-cargo-git

networks:
  default:
    labels:
      com.boilerplate-compile.managed: "true"
      com.boilerplate-compile.target: "windows"
      com.boilerplate-compile.project: "${PROJECT_ID}"
